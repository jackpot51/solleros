The password can be found in the Free/msg.asm file in the SVN trunk. This can be edited to suit your needs.
The default users/passwords are as follows
  || User || Password ||
  || root || awesomepower ||
  || user || password
  || n || `This password is blank` ||